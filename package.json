{
  "name": "chewhx.github.io",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch assets/styles.scss:assets/css/styles.css",
    "build:sass": "sass assets/scss:assets/css",
    "build": "npx eleventy",
    "watch:serve": "npx eleventy --serve",
    "serve": "npx eleventy --serve",
    "start": "npm-run-all build:sass --parallel watch:*",
    "debug": "DEBUG=* npx eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "axios": "^0.21.1",
    "moment": "^2.29.1"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.42.1"
  }
}
