{
  "name": "chewhx.github.io",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch scss/styles.scss:css/styles.css",
    "build:sass": "sass --no-source-map scss:css",
    "build": "npx eleventy",
    "watch:serve": "npx eleventy --serve --incremental",
    "serve": "npx eleventy --serve",
    "start": "concurrently \"npm run watch:sass\" \"npm run serve\"",
    "debug": "DEBUG=* npx eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.1.3",
    "axios": "^0.21.1",
    "bootstrap": "^5.1.3",
    "concurrently": "^6.4.0",
    "markdown-it": "^12.2.0",
    "moment": "^2.29.1",
    "sass": "^1.42.1"
  },
  "dependencies": {
    "journal-cli": "file:cli"
  }
}
